# 開発者への依頼メッセージ（案）

> 下記をそのままメール/チャットで送れます。

---

### ご依頼：政治家向けサイト candidate.seijiselect.jp の本実装（約1ヶ月）

お世話になっております。
政治家向けサイトのプロトタイプ（proto-v1）が固まりましたので、本実装をお願いしたくご連絡しました。
**作り直しではなく、完成済みのプロト画面を流用し、Supabase に繋いで Vercel で公開する**進め方を想定しています。1ヶ月程度を目安にしています。

**■ 対象**
- リポジトリ: `candidate-vote-match`（ブランチ `prototype` / タグ `proto-v1`）
- 仕様書: リポジトリ内 `docs/実装仕様書_政治家側_1ヶ月版.md`（スコープ・Supabaseテーブル定義・手順・受入条件を記載）
- 画面ごとの仕様: `PROTOTYPE_LOG.md` ／ 全体構想: `BACKLOG.md`
- 起動: `npm install` → `npx next dev`（画面は `app/prototype/` 配下）

**■ 進め方（重要）**
- 画面は完成済みなので**作り直さない**でください。
- バックエンドは作らず **Supabase**（DB＋認証＋ストレージ）、公開は **Vercel** を想定。
- 保存処理は **`app/prototype/_store.ts` の loadJSON/saveJSON に集約済み**です。基本は**この1ファイルの中身を Supabase に差し替えるだけ**で全画面が繋がります（key→テーブル対応表もファイル内に記載）。

**■ 今回の範囲**
- やる: 政策スタンス／クラファン作成／候補者プロフィール／支出・証憑／支援者CRM／支援募集／政策実装スコア／選挙活動タスク／定期献金管理／ダッシュボード集約 を Supabase 接続。ログイン・ファイルアップロード・Vercel公開。公開LPも配置。
- やらない（別フェーズ）: 寄付・献金の**実決済・本人確認・法令対応**、**実AI（LLM）接続**（AI画面はテンプレのまま）、スクレイピング、他サイト統合。

**■ 注意点**
- **お金を実際に動かす機能（寄付・献金決済）は今回入れない**でください（政治資金規正法・本人確認が絡むため、別途きちんと作る前提です）。記録・表示のみ。
- 個人情報（支援者情報・思想信条）は Supabase の RLS で「本人のみアクセス」を必ず設定。
- AI出力は「テンプレ（簡易版）」と画面に明記。LP・価格・実績はサンプルです。

お見積もり・進め方のご相談、よろしくお願いいたします。ご不明点があればお気軽にご連絡ください。
